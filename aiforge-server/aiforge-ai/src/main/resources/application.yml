spring:
  ai:
    # # Ollama 配置
    # ollama:
    #   base-url: http://localhost:11434
    #   chat:
    #     options:
    #       model: deepseek-r1:1.5b
    #       temperature: 0.7 # 控制发散程度
    # OpenAI 兼容配置
    openai:
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: qwen3.7-max-2026-06-08
          temperature: 0.7